FIFO（先进先出）:
1.新访问的数据插入队列尾部，数据在FIFO队列中顺序移动。
2.淘汰FIFO队列头部的数据。

LRU（最近最少使用）：
1.新数据插入链表的头部。
2.每当缓存的数据被命中时，则将数据移动到链表的头部。
3.当链表满的时候，将链表尾部的数据丢弃。

LFU（最近使用次数最少）：
1.新插入的数据插入到队列的尾部。
2.队列中的数据被访问后，引用的次数加一，队列重新排序。
3.当需要排序时，将将已经排序的列表最后的数据块删除。