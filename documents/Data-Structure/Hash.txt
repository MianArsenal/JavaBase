哈希表--哈希表是通过关键码去寻找值的数据映射结构，即通过关键码映射到值的存放位置来寻找值。

哈希冲突：
当两个不同关键码映射到同一个值时就会产生哈希冲突。

哈希冲突的解决方法：
开发地址法--当发生哈希冲突时，就根据一定的算法去寻找下一个空余的空间进行插入。
链地址法--当发生冲突时，就分配一个新的地址空间，以链表节点的形式进行插入。

复杂度：
select/insert/delete: O(1)(但是当所有的值都刚好在一个哈希值上时，就变成了链表，那查找就成了O(n))